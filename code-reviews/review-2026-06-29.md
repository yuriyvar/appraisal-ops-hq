# Code Review — 2026-06-29

**Reviewer:** Bob (automated weekend review)
**Repo:** appraisal-ops-hq (local-only)
**Scope:** 2 branches ahead of `main` + uncommitted working-tree changes

---

## Summary

Two branches are open: `feat/property-search-skill` (3 commits, the new property-search skill) and `kaizen/K-003-triage-sop` (1 commit, the triage SOP). Both have real defects that will surface during live use. The most urgent are two truncated files in the property-search branch — `SKILL.md` and `va-data-sources.md` both end mid-content, which will silently break Claude's GIS technique and leave the county lookup table empty. The triage SOP references a `/prep-today` command that does not yet exist, making its "both lanes share this SOP" guarantee false at merge time. The working tree has five uncommitted files with good operational content that should be committed.

---

## Branch: `feat/property-search-skill` (3 commits ahead of main)

**Commits:** `c2d11bf` (add property-search skill) → `0823d62` (populate VA county data) → `c02e368` (add DataMaster handoff step)

### Must-fix

**1. `skills/property-search/SKILL.md` is truncated — file ends mid-sentence**
The "Technique: county GIS via ArcGIS REST" section cuts off at line 93:
```
2. **Beware stale public mirrors**: prefer the endpoint the live app actually
   c
```
The sentence never completes, and whatever follows (likely the JavaScript technique, endpoint verification steps, and possibly a third bullet) is gone. This skill will malfunction on any county GIS lookup — the most common operation it exists to perform. The file needs to be completed before merging.

**2. `skills/property-search/references/va-data-sources.md` — counties table is empty**
The `## Counties / cities` section ends at the header row with no data:
```markdown
| Jurisdiction | Assessment | GIS / parcel map |
```
No county rows follow. The skill's Step 1 instructs Claude to "look up the county in `references/va-data-sources.md`" — with an empty table, every lookup silently falls through to the reconciliation path, which requires a live Chrome session. For counties already known (Hanover, Henrico, Richmond City, Spotsylvania, etc.), this is unnecessary friction on every run. The table that was ostensibly "populated" in commit `0823d62` ("populate VA county data sources from Ops sheet") has not actually landed in the file.

### Should-fix

**3. MLS routing column absent from `va-data-sources.md` and the main `county-registry.md` table**
The 2026-06-26 andon and inbox entries both flag this as a root cause of a real QA failure: Prince Edward and Mecklenburg/Kerr Lake are **Navica** markets, not CVR, and the county reference files have no MLS column to encode this. The implementation plan (Phase 2) lists adding a `MLS (CVR / Bright / Navica)` column and surrounding-county search sets as a committed item. This branch is the right place to land that work — the two files being added here are exactly where that column belongs. Leaving it out means the next rural Southside order hits the same wrong-MLS failure.

**4. `references/datamaster-handoff.md` — OneDrive path may become stale**
Line 5 hardcodes: `C:\Users\yuriy\OneDrive\Documents\DataMaster\` as the `.dma` file location, but the CLAUDE.md environment section notes the OneDrive backup decision is "pending (Sprint 1 item)." If OneDrive is removed, this path silently points nowhere. Suggest a one-line note: `(path subject to change pending Sprint 1 OneDrive decision)` or a TODO marker.

### Nice-to-have

**5. `property-search.md` command description is thin**
`.claude/commands/property-search.md` is six lines and passes `$ARGUMENTS` through to the skill. That's fine, but the `description:` field in `SKILL.md` frontmatter is where Claude Code triggers auto-routing — it's well-written. The command file could mention the MLS routing requirement ("ask if the county is Navica/Navica-adjacent") once the routing column lands, so the command wrapper sets context before the skill runs.

---

## Branch: `kaizen/K-003-triage-sop` (1 commit ahead of main)

**Commit:** `d0253e2` — promote SOP-triage v0.1 (two-view WIP triage)

### Must-fix

**6. `/prep-today` command referenced but does not exist**
The SOP (lines 65–67) states:
> `/prep-today` (Claude Code command) and the Cowork/Bob prep flow (`skills/worksheet-builder`) both read this SOP so the two lanes can't drift. `/prep-today` produces **View B**; its Step 3 wording must say "Alan to do = not on YV's list," NOT "skip."

There is no `.claude/commands/prep-today.md` in this branch or on `main`. The SOP's central claim — "both lanes reference this SOP" — is false at merge time for the Code lane. If the command doesn't exist, there is nothing to read the SOP and the cross-lane drift the kaizen was meant to fix is not actually fixed. Either create `prep-today.md` as part of this branch, or document that it is a Phase 4 item and note the SOP is live but `/prep-today` integration is pending.

### Passes

**7. Kaizen process followed correctly.** `K-003-5whys.md`, `K-003-pdca.md`, and the DRAFT file are all present in `vault/30-kaizen/`. The PDCA has a Check date (2026-06-30 — due tomorrow). The DRAFT file is correctly marked `status: DRAFT — staged in 30-kaizen for human merge`. Frontmatter fields (sop, version, effective, owner, last-kaizen, andon-count) are all populated. Cardinal rule #1 process was followed.

**8. SOP content is solid.** The two-view separation (View A = prep all WIP regardless of assignee; View B = YV's daily finalize list from Notes field) is clearly articulated. The column map (0-indexed) is explicit. The Google Sheet access method section correctly documents the sign-in fallback, JS tool restrictions (no `=` in output, size cap → paginate), and the "never use stale local XLSX" guardrail. The hard rule blocking `WIP - Review for Submission` from being worked is explicit.

**9. PDCA Check date is tomorrow.** If the merge happens before end of day today, the 2026-06-30 Check date gives a 24-hour feedback loop — appropriate for a process-change hypothesis. If merge is delayed, adjust the Check date accordingly.

---

## Uncommitted Working-Tree Changes

Five tracked files have unstaged modifications; three files are untracked.

**Should be committed (all clean, low-risk):**

- **`CLAUDE.md`** — adds `#appr`/`/appraise` trigger tag to rule 1.3. Correct, matches Phase 4 intent. Direct-commit path per conventions.
- **`interlane/INBOX-for-Code.md`** — five new memos (2026-06-22 through 06-26). Content is appropriate for this file; the 2026-06-26 QA failure [ACTION] memo in particular needs to be tracked so Code doesn't lose the renderer fix context.
- **`skills/property-search/references/county-registry.md`** — adds Charlotte Co and Buckingham Co rows to "Extended coverage" table with an MLS column. Well-formed, consistent with existing registry entries. Note: the main table above it still lacks an MLS column, but that's a pre-existing gap and these two new rows are correct.
- **`skills/property-search/references/data-quirks.md`** — adds `FLU-001` (Fluvanna actDataScout Tax ID hyphen format bug: screen shows `18A-1-353`, correct is `18-A-1-353`). Well-documented, follows table conventions, sourced to a confirmed real order.
- **`vault/00-inbox.md`** — seven new inbox entries including the 2026-06-26 andon (Tax ID renderer bug + Navica routing + 12-month window + meta "never delete YV's data" rule). All are appropriate capture; nothing here belongs in standard-work without a kaizen branch.

**Untracked docs — consider tracking:**

- `docs/2026-06-26_code-implementation-plan_consolidated-handoffs.md`
- `docs/code-brief_dma-value-corpus_2026-06-19.md`
- `docs/code-brief_worksheet-renderer-fixes_2026-06-19.md`

These are active work-direction documents for ongoing Code tasks (Phases 0–4). Leaving them untracked risks losing them between sessions. A simple `git add docs/` + commit with message `docs: add code briefs and consolidated implementation plan` would track them. If they're intentionally ephemeral (e.g., once Code completes Phase 0–4, they become obsolete), that's a reasonable call — but worth noting explicitly.

---

## Prioritized Recommendations

**P0 — Block merge until fixed:**
1. Complete the truncated `SKILL.md` (GIS technique step 2 onwards). *(feat/property-search-skill)*
2. Populate the empty `## Counties / cities` table in `va-data-sources.md`. *(feat/property-search-skill)*
3. Either create `.claude/commands/prep-today.md` or document its absence explicitly in the SOP. *(kaizen/K-003-triage-sop)*

**P1 — Fix in this sprint:**
4. Add MLS routing column (CVR/Bright/Navica) + surrounding-county search sets to `county-registry.md` and `va-data-sources.md`. This is Phase 2 of the implementation plan and the root cause of the Prince Edward/Mecklenburg QA failure. *(feat/property-search-skill, or a follow-on commit)*
5. Commit the five modified working-tree files. The FLU-001 data-quirks entry and county-registry extended coverage are particularly at risk of being lost or re-discovered.

**P2 — Nice-to-have:**
6. Add an OneDrive-pending caveat to `datamaster-handoff.md`. *(feat/property-search-skill)*
7. Track the three untracked docs, or explicitly decide to leave them ephemeral. *(working tree)*
8. After `/prep-today` is created, add a line to the command file pointing to `SOP-triage` so the SOP's cross-lane claim becomes true. *(kaizen/K-003-triage-sop follow-up)*
