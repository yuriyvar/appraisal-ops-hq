# Code Review — appraisal-ops-hq — 2026-06-13

_Automated weekend review (review-only; no files were committed, merged, or modified except this report)._

## Summary

There is one feature branch ahead of `main` (`feat/property-search-skill`, 4 files / +173 lines) plus uncommitted work on `main` (modified `CLAUDE.md` and `vault/00-inbox.md`, and several untracked files). The headline finding is a **data-integrity problem in the branch, not a logic bug**: the branch commit `c02e368` contains **truncated files** — `skills/property-search/SKILL.md` is cut off mid-word and `references/va-data-sources.md` is cut off at its table header with zero data rows. The complete, correct versions of those exact files exist only as **untracked files in the working tree on `main`**, so the good content is currently committed nowhere. Merging the branch as-is would ship a broken skill. The content itself (once you read the complete working-tree versions) is well-structured and security-aware; the issues below are mostly hygiene, a stale OneDrive path, and an unresolved repo-nesting question.

---

## Changeset 1 — Branch `feat/property-search-skill` (vs `main`)

Files in the commit: `.claude/commands/property-search.md`, `skills/property-search/SKILL.md`, `skills/property-search/references/datamaster-handoff.md`, `skills/property-search/references/va-data-sources.md`.

### Must-fix

1. **Committed `SKILL.md` is truncated (corner cut).** `git show feat/property-search-skill:skills/property-search/SKILL.md` ends at line 92 mid-sentence — `"...prefer the endpoint the live app actually c"` — with no trailing newline. Everything after that is missing from the commit: Technique steps 2–5 (stale-mirror guidance, `?f=json` schema inspection, the query pipeline, "record every endpoint discovery") **and the entire `## Rules` section** (the single-source-GLA rule, CAPTCHA policy, the Matrix "read-only, never bulk-scrape" guardrail). 92 lines / 5,900 bytes committed vs 110 lines / 7,067 bytes complete.

2. **Committed `va-data-sources.md` is truncated.** The commit ends right after the `## Counties / cities` table header (`| Jurisdiction | Assessment | GIS / parcel map |`, also no trailing newline) — **all ~45 county rows, the MLS table, and the National/Zillow table are missing** (28 lines committed vs 99 complete). A skill that resolves data sources from this file would find an empty table.

3. **The correct versions are untracked, not committed.** The complete `SKILL.md` and `va-data-sources.md` are present in the working tree but show as untracked under `?? skills/property-search/`. So the branch holds the broken copy and `main`'s working tree holds the good copy. Before this work is usable, the **complete files must be committed** (recommend re-committing the full files onto `feat/property-search-skill`, then verifying `git show <branch>:...| tail` ends at the real last line). Do not merge until the diff shows the full content.

4. **Stale OneDrive path in `datamaster-handoff.md`.** It hardcodes per-order `.dma` files at `C:\Users\yuriy\OneDrive\Documents\DataMaster\`. The latest `main` commit (`567125d`) and the pending `CLAUDE.md` edit both signal OneDrive is being removed ("Jobs folder path TBD after OneDrive removal"). This path will break post-migration. Confirm the post-OneDrive DataMaster project-file location and update the reference (and add a quirk note to the inbox per CLAUDE.md rule 2).

### Should-fix

5. **GLA thresholds are inconsistent across the skill and unexplained.** Inputs §4 uses a `±10%` band; Comp search §3 pre-filters at `±100 sf margin`; subject verification flags `>5%`; Rules define "verified" as agreement `within 5%`. Three different tolerances for three different purposes (search band vs. pre-filter margin vs. verification flag) is defensible, but as written a reader could apply the wrong one. Add one sentence distinguishing them.

6. **Unresolved data-quality TODOs shipped in `va-data-sources.md`.** Self-flagged in the file: Appomattox Co and Bedford Co list the *identical* ArcGIS Experience URL ("verify which is right"); Louisa is misspelled "Lousa" in the source sheet; Fluvanna has two conflicting assessment sources. Low urgency, but resolve before relying on those jurisdictions on a live order.

### Nice-to-have

7. **No worked example / smoke test for the ArcGIS pipeline.** The Technique section describes a non-trivial query flow (resource sniffing, `?f=json` schema discovery, pagination at `maxRecordCount`, GPIN `IN`-chunk batching, haversine). A single runnable example (e.g. the Hanover `maps.civ.quest/.../Hanover/Public/FeatureServer` query) would let a future session confirm the endpoint is still live before depending on it. This repo has no test harness, so unit tests are N/A — a canonical example is the practical substitute.

### Positives

- Branch name `feat/property-search-skill` follows the CLAUDE.md `feat/*` convention; the SKILL.md frontmatter matches the required `name` + pushy `description` format.
- Security posture is good: the skill scopes the licensed Matrix session to read-only ("never bulk-scrape, never act beyond running searches"), declines CAPTCHA solving, confirms before downloading exports, and asks before writing to the Google Sheet. It also re-encodes the repo's cardinal rules (no client/order data in the repo; save outputs to the job folder; andon on broken sources; append quirks to the inbox).
- No secrets committed (CLAUDE.md rule 5 holds). Note only: the Operational Records Google Sheet doc ID + gid are embedded in two files — these are access-controlled resource identifiers, not credentials, so this is informational rather than a violation.

---

## Changeset 2 — Uncommitted changes on `main`

### `CLAUDE.md` (modified)

8. **Jobs folder now equals the repo's parent — confirm this is intended.** The edit sets *Client job files* to `C:\Users\yuriy\VDV Appraisals\`, which is the **parent directory of the repo itself** (`C:\Users\yuriy\VDV Appraisals\appraisal-ops-hq\`). The cardinal rule is that client/order data must never enter the repo, yet the repo now lives *nested inside* the client-data folder. The property-search skill saves outputs to `...\VDV Appraisals\<job>` (siblings of the repo), which works, but the nesting is fragile: a recursive backup/copy of the jobs folder would sweep in the whole `.git` repo, and the "no client data in repo" boundary now depends on path discipline alone. Recommend either a dedicated jobs subfolder clearly separate from the repo, or an explicit note in CLAUDE.md acknowledging the nesting and how the boundary is enforced. Must-verify, low effort.

9. **Minor doc drift:** the new `property-search` skill is not added to the `## Layout` description of `skills/` (still lists only `memory-capture`, `memory-review`, `kaizen-retro`). Add it for discoverability.

### `vault/00-inbox.md` (modified)

10. **Conforms well to capture conventions** — ten new learnings, newest-first at the top, one line each, with `[learn]` prefix tags, per CLAUDE.md rule 2. Good. Two of them explicitly point to destinations ("→ belongs in datamaster-handoff reference", "→ candidate for property-search SKILL"); those are exactly the items the OneDrive-path fix (#4) and a future session-start skill should absorb during the weekly memory-review. The VPN-off-before-DataMaster-export learning is operationally important and should make it into `datamaster-handoff.md`.

### Untracked files

11. **`Scheduled/weekend-pr-review/SKILL.md`** is sitting untracked in the working tree. It's this review task's own definition; decide whether `Scheduled/` belongs in the repo (and in `.gitignore` if not). Not part of the feature work.
12. **`.claude/commands/property-search.md`** appears both as untracked on `main` and as a committed file on the branch — consistent content, just the same branch/worktree duplication noted in #3.

---

## Prioritized recommendations

1. **(Must)** Do not merge `feat/property-search-skill` until the truncated `SKILL.md` and `va-data-sources.md` are re-committed in full. Re-commit the complete working-tree versions onto the branch, then verify the committed copies end at their true last line and have trailing newlines (#1, #2, #3).
2. **(Must)** Reconcile the OneDrive DataMaster path in `datamaster-handoff.md` with the in-progress OneDrive removal; update the reference and log the quirk to the inbox (#4).
3. **(Should)** Confirm the repo-nested-inside-jobs-folder layout is intended and document/enforce the client-data boundary (#8).
4. **(Should)** Add one line disambiguating the ±10% / ±100 sf / 5% GLA thresholds (#5); resolve the Appomattox/Bedford duplicate URL and "Lousa" typo (#6).
5. **(Nice)** Add a runnable Hanover ArcGIS example as a smoke test (#7); add `property-search` to the CLAUDE.md layout list (#9); fold the VPN-off and Sale-Date-from-History inbox learnings into `datamaster-handoff.md` (#10); decide whether `Scheduled/` is tracked (#11).

---

### Reviewer notes
- A `.git/index.lock` file was present during this review and could not be removed from the review environment. If it is stale on the workstation it will block commits — worth deleting if no git process is running.
- Review scope was the diff `main...feat/property-search-skill` plus `git status`/`git diff`/untracked content on `main`. No code was modified, committed, merged, or pushed.
