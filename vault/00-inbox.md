# Inbox — append and review

Rules: newest entries at the TOP, directly under this header. One line minimum,
real example attached when possible (genchi genbutsu). Never edit old entries —
the weekly review processes and removes them. Prefix tags are optional but help:
[learn] [problem] [idea] [client] [andon]

---

- 2026-07-06 [learn] **BD4 MCP server LIVE-VERIFIED in BOTH lanes → observation week RUNNING (→ ~7/13)** — Cowork-lane wiring = a `mcpServers` block in `%APPDATA%\Claude\claude_desktop_config.json` (same command/args as the outer `.mcp.json`; the file had none before — Desktop merges it fine next to `coworkUserFilesPath`/`preferences`). Ton's session: `county_route` Chesterfield + `cache_lookup` Hall Rd clean MISS = host-side sqlite read through the server, the exact op his sandbox can't do (7/04 andon workaround now permanent). Code lane: README checklist 7/7 incl. `resolve_subject` synthetic → 3 artifacts, Narnia isError + coverage list. Side quirk: first Cowork start after the config edit threw "VM service not running" — `CoworkVMService` was actually Running; a full quit→relaunch cleared it (transient, likely service-not-ready-yet race; reboot is the fallback).
- 2026-07-04 [problem] **`ingest_subject.py`'s Chesterfield APN dash-strip fires on non-numeric placeholder text, not just real dashed parcel IDs** — on 14632 Hancock Towns Dr, `identifiers.apn` was set to a descriptive placeholder ("to be determined (per MLS #2615512 - no parcel assigned yet, matches CHE-003)") since there's genuinely no parcel yet; the Chesterfield rule (`"-" in str(apn)` -> strip dashes) matched the placeholder's punctuation and mangled `assessors_parcel_number` into garbled text ("CHE003", missing the explanatory dash). Harmless (caught + hand-fixed this time) but the check should require `apn` to look like a real Chesterfield parcel number (e.g. digits-with-dashes only) before stripping, or the resolver/ingest skeleton should represent "no parcel yet" as `null` + a `no_tax_id` flag ONLY (which it already has in `resolution.no_tax_id`) rather than also stuffing prose into `identifiers.apn`.
- 2026-07-04 [learn] **CHE-003/BLD-001 CONFIRMED LIVE on 14632 Hancock Towns Dr (order 68146204, 1004+1007, due 7/6)** — direct Chesterfield ArcGIS `Cadastral_ProdA/FeatureServer/3` query for `StreetName LIKE '%HANCOCK%'` returns NO record for house number 14632. The even-address side of Hancock Towns Dr is still partly under **Hancock Village Building Company LLC**'s undivided builder parcels (TaxID `722670030900133` @ 14630 and `722670030900134` @ 14648 — both `UseCode: null`, `FinishedArea: 0`); 14632 falls inside that unplatted gap. Nearest platted same-complex comps (odd side, from the neighbor order's CSV `Comps files\14640-Hancock-Towns-Dr_comps_appraiser-single-line.csv`): 14649/14651/14657/14661 Hancock Towns Dr, all 1532 sf / 3bd-2.1ba / 7 rooms, built 2025-2026, sold $359–377k. **Do not use as subject GLA** — confirm the actual unit at inspection/MLS; flagged null in the skeleton per the no-guess gate. Also flagged: order form is **1004+1007** (fee-simple + rent schedule) but the complex's UseCodes are CD/CN (condo) — the neighbor unit 14640 was appraised as **1073 Condo**; worth confirming the form choice with the client/lender before building the worksheet. Bonus technique note: ArcGIS `outFields` field-name filter must use the FeatureServer's real field names (`Address`/`HouseNum`/`StreetName`, not `SITEADDRESS`); `returnGeometry=false` is required or the response blows past web_fetch's token cap (raw response saved to a truncated tool-result file otherwise).
- 2026-07-04 [problem] **Cowork's Write tool cannot create new subdirectories** — it can only write into a folder that already exists in the connected-folder whitelist (confirmed: writing to `outputs/` root succeeds; writing to a brand-new `outputs/sr_tools/...` or `outputs/appraisal-ops-hq/...` path fails "outside this session's connected folders" even though `outputs/` itself is connected). Workaround: `Write` the file flat into an already-connected root, then use `mcp__workspace__bash` `mkdir -p` + `cp` (same mount, not the buggy repo mount) to relocate it into a nested structure.
- 2026-07-04 [problem] **The bash-mount stale/truncated-read quirk (2026-06-13) reproduced on a FRESH file, not just Edited ones** — copied `tools/subject-resolution/resolve_subject.py` (via `Read`+`Write`, not `Edit`) into the sandbox's own `outputs/` mount; bash/python reads of that exact path were truncated mid-token at a byte count matching an OLDER revision of the file, even after re-`Write`ing the longer, current version twice. The `Read` tool (host view) showed the correct/complete file throughout. **Fix that worked:** write under a brand-new filename (`resolve_subject_v2.py`) instead of re-writing the same path — that read clean immediately. Suspect the mount caches by inode/path rather than content hash; re-Write-same-path does not reliably invalidate it, but a new path does. Add to data-quirks / Code investigation queue alongside the existing Build-C sqlite andon.
- 2026-07-04 [andon] **Build C's sqlite subject-cache is UNREACHABLE from Cowork against mounted folders** (order: 14719 Clover Ridge Ln, Chesterfield). `resolve_subject.py`'s `cache_get()` unconditionally does a `CREATE TABLE IF NOT EXISTS` write before it can even read, and that write fails `sqlite3.OperationalError: disk I/O error` on EVERY mounted path tried — the real `VDV Appraisals\Subject cache\` AND the sandbox's own outputs mount. Plain-file reads/writes on these mounts are fine; only live sqlite (needs POSIX/file locking) breaks — consistent with a FUSE/network-mount limitation, not a data bug. Confirmed: sqlite reads-only (SELECT on an existing file, e.g. `va-gas-providers.sqlite`) DO work over the same mount; it's specifically schema-creation/INSERT that fails. Workaround used this session: ran the resolver's MISS-path logic directly (bypassing `cache_get`/`cache_put`; safe here since it's a brand-new address, never cached before) and called `ingest_subject.py --no-cache`. **This means the entire Build-C cache is a dead letter for every Cowork-run order until fixed** — either (a) Code confirms whether the live host (non-sandboxed, real Claude Code CLI) has the same mount limitation, or (b) the cache needs to live somewhere Cowork's sandbox can write live sqlite (e.g. `/tmp` synced out some other way) or be swapped for a plain-JSON-file cache (no locking needed) if Cowork is expected to drive resolution regularly. **→ Code: please investigate + reply in INBOX-for-Cowork.md** (memo posted same session, INBOX-for-Code.md).
- 2026-07-04 [problem] **`tools/subject-resolution/resolve_subject.py` reads TRUNCATED via Cowork's bash mount** (cuts off mid-string at line 388 of 426, i.e. ~91% through the file) — confirmed NOT a content issue (the Read tool / host view shows the file complete and syntactically valid; re-Writing the identical content back to the same path did not fix the bash-side truncation). Sibling files in the same directory (`subject_cache.py` 284 ln, `ingest_subject.py` 350 ln) read PERFECTLY via bash at the same time — this is isolated to one specific file, cause unknown (not simply "recently edited," since re-writing didn't clear it). Same family as the 2026-06-13 "Cowork mount sync is one-directional+laggy" quirk, but that workaround (re-Write) did NOT resolve this instance. **Workaround that DID work:** copy the tool + its sibling deps into the sandbox's own outputs mount (always reads fresh) and run from there, pointing `--out-dir`/`--db`/`--out` back at the real client-zone path (plain-file writes to the mount are reliable). If this recurs, don't assume re-Write fixes it — verify with `wc -l` + `ast.parse` before trusting a bash-side read of a repo tool.

- 2026-07-04 [done] **BD4 SHIPPED — `appraisal-data` MCP server** (roadmap Track 4, `mcp/appraisal-data/`): bare-stdlib stdio JSON-RPC shim (`mcp_stdio.py`, tools-only subset, rev 2024-11-05, ~200 lines, **separate from the tool table** — SDK swap = one module; triggers pre-agreed) + `server.py` with **8 tools** (resolve_subject · ingest_subject · cache_lookup READ-ONLY · gas_lookup · county_route · comp_history_search · arcgis_fetch · add_county), every reply ends in a NEXT step, print-heavy CLIs subprocessed so **stdout stays pure protocol**, cache write path stays ingest-only. Registered in `VDV Appraisals\.mcp.json` (outer root, same discovery rule as slash stubs; loads at session START). QA 2/2 (shim transcript + real-subprocess e2e w/ temp DBs). **LIVE VERIFY = first act of the next session** (checklist in the README).
- 2026-07-04 [learn] **Repo `.claude\commands\` is INVISIBLE to sessions** (they root at `VDV Appraisals\`, one level up) — that's why `/build-day` "didn't work in any session" while `/prep-today` (outer dir) always did. Fix pattern: 3-line **pointer stubs** in the outer `..\.claude\commands\` that read+execute the versioned repo command (logic never duplicated). Stubs created for build-day/appraise/resolve-subject; **every future user-invocable repo command needs its stub** (note added to the repo build-day.md header).
- 2026-07-04 [learn] **Sandboxed `git status` leaves a stale `.git/index.lock` it can't unlink** (Ton's weekend review run, 06:56 — blocked host git until Code deleted the 0-byte lock same day). Prevention: automated/sandboxed reviews run read-only git with `GIT_OPTIONAL_LOCKS=0`. Ton's review also re-flags the standing YV gates: K-003 ready for human merge; `feat/property-search-skill` superseded → delete decision.
- 2026-07-04 [done] **BD3 SHIPPED — historical comp recall** (roadmap Track 3): client-zone index `Past Reports\_analysis\comp-history.sqlite` joins **572 Ops orders (2025+2026 tabs, pulled live via Chrome/gviz) + 113 June-corpus subject facts + 191 .dma filenames/mtimes** on one squashed street key (shared canon w/ the subject cache) = 592 rows, 20 orphans. Resolver now surfaces a **"Prior work"** section every order (pull sheet on miss, prior-work.md on hit): SAME-property history any-date + zip/GLA-±15% similars ≤12 mo — **CANDIDATES for YV only**, close dates re-verified in MLS always. **DMA-004 quirk filed:** structured per-comp .dma extraction DEFERRED (value slots carry unique IDs the schema names don't reference; June's pairing script never committed) → index carries one-comp hints; open the .dma in DM for the full grid. Ops-tab layout is positional (col0 date · 3 street · 4 zip · 5 form · 6 due/done · 10 value · 11 status). Weekly /review Phase-4 now refreshes the index. QA 5/5 + 20/20 + 22/22.
- 2026-07-02 [done] **BD2 SHIPPED — multi-source verification + variance protocol** (roadmap Track 2, commits 652241d..P3): pull order now **MLS → County/GIS → Zillow** (pull sheet resequenced + skeleton ships `source_values.<field>.{mls,county,zillow}` slots for gla/year-built/lot/beds/baths/stories + `variance_notes`); ingest resolves per **YV's protocol** — agree → county governs quietly; disagree + listing-supported reason → **MLS governs WITH the justification**; disagree unsupported → **County rules**; either way an "inconsistent — manual triage" chip lands on the verification row AND the worksheet header until YV clears (clear = re-ingest with the reason or the corrected value, never hand-delete). Zillow never governs. QA 19/19 + 22/22, e2e chip render proven.
- 2026-07-02 [done] **BD1 SHIPPED — standard-work enforcement** (automation-roadmap Track 1, commits 5955446..P4): every `/resolve-subject` now writes **run-log.md** (tools tick their own steps — resolver 1, ingester 3; humans tick 2/4; unchecked boxes = weekly `/review` Phase-4 audit finds them); **provenance chips, warn-loud-never-block** — ingest without a pull-sheet sibling flags "standard work not verified", a subject.json with no resolution stamp (bypassed ingest) gets "produced outside standard work" ON THE WORKSHEET HEADER; **add_county.py** adds registry row + routing entry together or not at all (drift rule mechanical, dup rows refused, rollback on partial failure); `/appraise` command = the ONE door into the order lane (no portal browsing before HIT/MISS). QA 17/17 + 22/22.
- 2026-07-02 [done] **Build C SHIPPED — subject-resolution + cache** (`tools/subject-resolution/`, commits 0355044..P5): cache-first `/resolve-subject` (SQLite in CLIENT zone `Subject cache\`; staleness = FLAG at >180d / tax-year-behind, vintage preserved, never silently reused) → miss emits a v1.1 all-null skeleton + per-order **pull-sheet.md** (SOR vendor/URL/technique from `county_routing.json` [12 jurisdictions, same-commit drift rule with county-registry.md] + Source-1/2/3 checklist + gas-DB answer incl. confirmed-absent + Navica both-accounts + surrounding-county sets) → `ingest_subject.py` = the ONLY cache write path (numeric cleaning, baths N.M split, lot sf↔ac >2% flag, Chesterfield APN dash-strip, Zillow photo-derived flags, county-vs-MLS GLA verification row) → assemble→render unchanged. Stretch landed too: `fetch_arcgis.py` (Chesterfield/Hanover FeatureServer, one-match-only, fill-nulls-only, FIELD_MAPS **UNVERIFIED until first live pull**, any failure = loud fallback). QA runner 14/14 (no network) + Build B 21/21 intact. Repeat orders on a subject are now a cache hit.
- 2026-07-02 [done] **6/19 renderer/assembler brief FULLY IMPLEMENTED — the automated `assemble→render` path now emits the adopted worksheet standard** (build day, commits dbef532..P4): schema **v1.1** (subject += assessors_parcel_number · map_reference "GIS" dflt · walls_trim "Wood" dflt · water/sewer · re_taxes_annual · hoa_amount/period · neighborhood_bounds · description context; order += contract{}; market.search += surrounding_counties); renderer subject tab DM-ready (one "Assessor's Parcel # ★" row, Map Ref ★, DEFAULT-chip walls, TBD-never-guess utilities, ▶ IMPROVEMENTS banner, R.E. Taxes ★ own row, HOA ★ always, Contract block); NEW **Neighborhood tab** (boundaries/description templates, land-use 0% SFR dflts, one-unit price/age derived from ≥3 closed comps) + **search-snapshot strip**; **12-mo window flag demoted to INFO when sale_date merely uncaptured** (hard only when a real date is >12mo). QA 17→**21** tests green, double-render byte-identical. Fixture pair `appraisal-record.example.json`(+.html) now actually committed (README had referenced a file that never existed).
- 2026-07-02 [learn] **DM registry has NO URAR Neighborhood-trend fields** (quirk DMA-003): verified the new Neighborhood/Contract collection-sheet rows against the 113-file `.dma` corpus registry — DM registers only `ContractDate(Known/Unknown)` · `HasConcessions/ConcessionAmount/FinancialAssistanceUnknownAmount` · `FinanceTypes` · `ChangeInLandUse_*` · `MarketAreaVacant_0To5Percent`. Built-up/growth/values/demand/marketing-time/boundaries/land-use-%/one-unit-housing (+ contract price, seller-is-owner) are **ACI-direct entry**. Collection-sheet CATALOG corrected to verified names (invented ones removed); regenerated.
- 2026-07-02 [done] **Gas DB now distinguishes "confirmed absent" from "not looked up"**: sentinel provider row (id 90, `lookup_method=confirmed_absent`) + county rows for **Charlotte · Buckingham · Mecklenburg** (all live-order verified, rural Southside). Query hit ⇒ Gas: Not available ⇒ propane/electric/heat-pump, never a gas-map check. **Powhatan checked for 2013 Oneida: NO row — genuinely not yet looked up → gas unknown, confirm at inspection** (subject is well/septic per Matrix anyway). Rebuilt: 10 providers / 71 county rows.
- 2026-07-01 [done] **DM collection-sheet remapped to the 4 DM/ACI tabs** (Subject +Contract · Neighborhood · Site +parcel dims · Improvements) + a top **search-snapshot** block (GLA, +/-10% comp range, garage/carport, basement, surrounding counties). `tools/dm-collection-sheet/build_collection_sheet.py` now 76 fields (41 gap-flagged); the previously-MISSING **Neighborhood** section (11 fields) + a **Contract** sub-block (5) added; prefill + all 12 tokens intact throughout. Phased commits e58e3f8..2d76d16 (brief `docs/2026-07-01_dm-tabs-remap_claude-code-brief.md`); deliverable regenerated to `Operations/Template files/`. **DEFERRED (token budget):** field-map `aci_tab` seed (aci_web null till ACI live) + verify the new Neighborhood/Contract DM field NAMES vs the `.dma` registry.
- 2026-06-30 [client] (YV rule) **Navica comp extraction — pull the FULL comp-grid set per comp**, not just price/GLA/beds. Required fields: total room count · beds · baths · **Pending (under-contract) date** · type of house/style · **heating/cooling** · **porch/deck/patio** · **basement total + finished/unfinished** · **garage/carport (type + #)** · **financing type + seller-paid closing costs (concessions)**. Several (heating/cooling, basement finish, garage, porch/deck/patio) aren't in Navica's field summary → read the property description/remarks + photos; flag `⚠ confirm` if absent, never guess. Baked into `skills/property-search/references/navica-accounts.md` → "Per-comp data to extract" checklist.
- 2026-06-30 [learn] **NAVICA COMP PULL — FULL WORKFLOW DISCOVERED** (live order: 4237 Hall Rd, Mecklenburg Co):
  1. **Two accounts needed for Mecklenburg**: South Central Assn of Realtors (acct 287, `next.navicamls.net/287`) AND Southern Piedmont Land & Lake Assn of Realtors (acct 397, `next.navicamls.net/397`). Search BOTH — comps split across systems. Registry file: `skills/property-search/references/navica-accounts.md`.
  2. **MLS# search → POST via `MlsNosForm`**: navigate to `/397/Search/Index?accountNo=397`, fill `input[name="mlsNo"]` fields (numeric only, e.g. `70888` not `R70888C`), submit `#MlsNosForm` (POST) → lands on `/397/Search/MlsListToResults` with a results table.
  3. **Listing detail → `/397/Expanded/Single`**: click the listing photo/row in results (delegated click handler on TR) → opens `/397/Expanded/Single`. Fields available: Selling Price, Closing Date, Financing, Seller Paid Conc, Style, Rooms, Beds, Baths, SqFt Range, **Est Total Htd SqFt** (= GLA), Year Built, Total Acreage, Tax Parcel ID, Taxes, Deed/Plat refs, HOA, Waterfront, Views.
  4. **Quick search (`searchValRC` field + GET) fails with 500** — always use `MlsNosForm` POST instead.
  5. **Layout: Traditional** (via layout dropdown `ref_218`) shows all fields; Slim Line shows fewer columns.
  6. **Credentials** in `Operations/Navica MLS basics.docx` — do NOT put in registry files.
  7. **Detailed search workflow** in `Operations/Navica MLS basics.docx` — status: Active + Pending + Closed; broad location (rural markets cross county lines); Closing Date 6–12 mo; sort by Distance + Close Date.

- 2026-06-30 [learn] **NAVICA — South Central vs Southern Piedmont**: South Central (287) is a PARTICIPANT BOARD within Southern Piedmont (397) shared search. Despite this, each has its own listings that don't fully cross-populate — must search both independently. MLS# prefix pattern: R57xxx = South Central; R70xxx–R71xxx = Southern Piedmont.

- 2026-06-30 [learn] **Mecklenburg County comp verified**: 6112 Tillman Rd (R71476C) — SP $195,000, Close 11/12/2025, 3/2, 1,188 sf Ranch, 2020, 1.51 ac, Brunswick Co, AG zone, No HOA, Seller conc $0. Pulled from Southern Piedmont (397).

- 2026-06-30 [learn] **4237 Hall Rd subject**: GLA = 1,250 sf double-wide (from client — county CAMA has no building record). Manufactured home — flag on 2055. Coords: 36.6461179, -78.0902793. La Crosse, VA 23950, Mecklenburg Co. AG zone, 2.08 ac, Electric/Well/Septic, no gas.

- 2026-06-26 [andon] (YV, sharp) **Worksheet QA failures on 3542 Heights School Rd + 63 The Moorings + 1 Brockenbrough — fix and never repeat:**
  1. **Tax ID/APN missing in rendered HTML.** Root cause = renderer bug: `COMP_ROWS` in `tools/worksheet-renderer/render_worksheet.py` rendered MLS# but had NO comp Tax ID row; subject "APN/Tax ID" only read `ids.apn` (we store `pid`/`map_id`). FIXED 2026-06-26: added comp "Tax ID (PID/APN)" row + subject APN falls back to pid/map_id. **→ Code: commit this renderer change; add a gate check that every comp's Tax ID actually appears in the HTML, not just the JSON.**
  2. **MLS-MARKET ROUTING — NAVICA.** Prince Edward (3542) and Mecklenburg/Kerr Lake (The Moorings) are **NAVICA (Lake Country Assn of Realtors)** markets — NOT CVR. I knew this yet (a) pulled CVR comps for PE and (b) didn't give YV the **surrounding counties/zips to search in Navica**. Rule: for a Navica market, always include the **adjacent-county search set** (PE → Buckingham, Appomattox, Charlotte, Cumberland, Nottoway, Lunenburg; Mecklenburg/Kerr Lake → Lunenburg, Charlotte, Halifax, Brunswick + NC Kerr Lake shore: Vance/Granville/Warren NC). **→ add a `va-data-sources` MLS-by-county map (CVR vs Bright vs Navica) + surrounding-county sets to county-registry.**
  3. **12-MONTH SALES WINDOW.** The Moorings candidates included sales well outside 12 months (Unit 60 05/2023, Unit 51 10/2024, Unit 31 02/2024, Unit 8 12/2024). Default comp window is **12 months**; older same-project sales are SUPPLEMENTAL ONLY with an explicit dated-sale justification — never primary, never unlabeled.
  4. **META (the big one): WHEN IN DOUBT, ASK — and NEVER delete/strip YV's data.** When YV said "leave the comps for me," I wrongly **removed** the comp grids from both records. Correct read: keep what I found as CANDIDATES for YV to confirm + supplement. Do not destroy work on an ambiguous instruction; ask first.
- 2026-06-23 [client] (YV rule) **COMP-SELECTION RUBRIC** — bake into worksheet-builder gate + comp-selection logic:
  (1) Above-grade living area **±10%**.  (2) Distance tiers — **Rural: 1,3,5,7,10,12 mi** (start tight, expand outward only as needed); **Urban: 0.25,0.5,1,1.5 mi**; **Suburban: urban tiers up to 3 mi**.  (3) Parcel/lot area **±20–30%**.  (4) Sold-price variation **3–15%**.  (5) **IF ANY criterion exceeds its band → HIGHLIGHT it and let YV decide** — never silently drop or include. The 'if more' rule applies to ALL criteria (living area, distance, parcel, price, etc.).
- 2026-06-23 [learn] NEW-CONSTRUCTION comp nuance (SITUATIONAL, not a rule-override; YV agreed the local set was correct): the subject builder's other recent new sales are a **supplemental/fallback** comp source ONLY when local new-construction comps are genuinely inadequate. **Local same-market sold comps remain PRIMARY when available** — they were here (Nottoway). Builder comps 50–70 mi away (Gordonsville/Charlottesville) sit in different markets and need large location adjustments, so they're a last resort, not a default. Lesson for Bob: hold a sound analysis with confidence; user pushback ≠ proof of error.
- 2026-06-18 [learn] PROCESS: **Gas utility lookup is a viable SOP step for Henrico (and likely other counties served by Richmond Gas Works).** Navigate to `https://cor.maps.arcgis.com/apps/webappviewer/index.html?id=32fb679315c0451e88f80402a4deb1a5`, search the address, then click on the street/parcel area (NOT the search result pin) to trigger the gas availability popup. Result for 1214 Hillside Ave: "Gas available but not currently at this location — can be brought to it." → forced air heating is NOT gas (likely heat pump or oil). Confirm fuel at inspection. → add Richmond Gas Works ArcGIS URL to county-registry Henrico row + worksheet-builder subject checklist.
- 2026-06-18 [learn] PROCESS: **When starting a new order, pull ZIP (col 4) from the Ops tracker at the same time as the address** — needed immediately to resolve county + correct SOR. Don't navigate blind to Zillow with just a street name; ZIP narrows it to one result. Example: 1214 Hillside Ave ZIP 23229 → Henrico → APEX. Add ZIP pull to `/prep-today` Step 4 cross-reference output.

- 2026-06-17 [learn] PROCESS: **Zoning needs the DESCRIPTION, not just the code** (e.g. Henrico R-4 = 'One-Family Residence District'). Pull/Map the zoning code AND its district description into the subject. → worksheet-builder subject checklist + field-map (Site/Zoning).
- 2026-06-17 [learn] PROCESS: **Capture Natural Gas service/availability** in the subject Utilities (1004 Site: Electricity/Gas/Water/Sewer). Source from heating-fuel (gas furnace ⇒ gas), MLS, or utility-area; flag 'confirm at inspection' if unknown. Was missing.
- 2026-06-17 [learn] (YV) **Comp inference: distance is NOT definitive.** Don't rank/select comps by proximity alone — weight **sale price + prior/last sale** (and condition/DOM) as primary inference signals; a slightly-farther but truer-condition/price comp can beat a closer one. → worksheet-builder gate + comp-selection logic: surface price + prior-sale prominently; distance is one factor.
- 2026-06-17 [client] (YV rule) Appraisal working files (subject.json, appraisal-record.json, worksheet.html, comp CSVs) go in `C:\Users\yuriy\VDV Appraisals\Working Subj & Comps files\` — per-appraisal subfolders. Supersedes ad-hoc per-order top-level folders.
- 2026-06-17 [learn] PROCESS FIX: comps MUST capture **ML# + Tax ID (PID/GPIN)** — Bob pulled only address/GLA/beds/price on the Fairmount comps and omitted ML#/PID (DataMaster needs them). When scraping the Matrix Appraiser-Single-Line grid, ALWAYS take the ML# + PID columns. Enforced in worksheet-builder gate (per-comp data) + property-search Step 4.
- 2026-06-17 [learn] CAUSE of the actDataScout block above = **VPN** (known Richmond issue — actDataScout blocks VPN/datacenter IPs, serves Blocked.html). Off-VPN it loads normally. RULE: before any Richmond City (actDataScout) pull, VPN must be OFF.
- 2026-06-17 [problem] Richmond City SOR = actDataScout (legacy apps.richmondgov.com redirects to it), but actDataScout **bot-blocks** automated browser access (serves /static/BlockedAccess/Blocked.html). So Cowork can't pull Richmond City CAMA (GLA/year/beds/sales) directly. Workarounds: (a) Yuriy opens actDataScout (eligible user) and Bob reads the card, or (b) Matrix **Tax Search** tab (public-records cross-ref) once logged in. GIS `richmondvagis/Parcels` gives only land/assessment values, not building chars. → update county-registry Richmond City row with this access note.
- 2026-06-15 [client] (YV) **Organize ALL DM fields by DM/ACI TABS** — Subject · Neighborhood · Site · Improvements — so the worksheet is used tab-by-tab with no hunting (and seeds web-ACI auto-fill next month, since ACI web tabs == these sections). **TOMORROW:** remap `tools/dm-collection-sheet/build_collection_sheet.py` CATALOG into these 4 tabs (+Contract sub-block); **ADD the missing Neighborhood section** (location type, built-up %, growth, values trend, demand/supply, marketing time, present land-use %, boundaries, market conditions); regenerate template+reference; keep source hints + ★ gap markers + prefill token map; then populate field-map `aci_web` column from the same structure. Full spec in `.claude/Session-Handoffs/SESSION-HANDOFF_2026-06-15.md` (session-3 addendum, PRIORITY 1).
- 2026-06-15 [done] **Pick-a-tier worksheet pre-fill** (YV's requested feature): `tools/dm-collection-sheet/prefill_worksheet.py --form <1004|2055|gpar|1073> --tier <new|average|fair>` → writes the DM-complete worksheet with that tier's Improvements + Q/C defaults filled (amber "verify" markers; order tokens left blank; banner flags grounded vs extrapolated). Stdlib-only — includes a tiny YAML-subset reader (no PyYAML; nothing in repo imports yaml — confirmed). Parser bug found+fixed during test: inline `# comment` on a nested-map key (`exterior:  # OBSERVED`) made children flatten → strip comment before the branch decision; 2055 now fills 11 fields not 3. 14/14 parser checks + 5 sample renders pass.
- 2026-06-15 [done] Built **condition-profile datasets** (default Improvements + comp Q/C values by tier) for 1004, 2055, gPAR, 1073 × {new, average, fair} → `skills/worksheet-builder/references/condition-profiles/` (+ `_model.md`). Grounded in corpus (38 XML + 22 gPAR): Quality Q3 dominant (no Q5/Q6), Condition C3 modal, rating suffix `/Avg` ×326 + `/New` ×29 — **no `/Fair` or `/Poor` exists**, so the FAIR tier (and all of 1073 — no condo XML) is EXTRAPOLATED from UAD convention and flagged `observed_in_corpus: false`. UAD forms use Q/C + `material/rating`; gPAR uses words (Good/Avg/Fair/Poor). Pasted as a scaffold; appraiser edits + itemizes C5. Wired into worksheet-builder SKILL. Quality≠condition note included (grade vs wear).
- 2026-06-16 [client] (YV rule) **Interlane mailbox — "the Cowork-Fixon"** (`interlane/`). Dedicated bidirectional Cowork⇄Code memo channel: two one-way inboxes (`INBOX-for-Code.md` Bob→Code · `INBOX-for-Cowork.md` Code→Bob), single-writer each (no cross-lane same-file edits). Each lane reads the inbox addressed to it at session start; **reciprocation mandatory** (reply in sender's inbox + mark [DONE]). Distinct from Session-Handoffs (cold-start) and this inbox (learnings/kaizen). Supersedes ad-hoc `[andon→Code]` vault pointers. Wiring into START-HERE is queued for Code (brief Task 2A.5 + INBOX-for-Code memo).
- 2026-06-16 [client] (YV rule) **Delegate-to-Code protocol.** When Cowork needs something it can't do/reach (git writes, `.claude/` edits, Code memory, kaizen-branch SOP changes, host ops), Bob does NOT force it or work around the guardrail → (1) writes a `docs/<date>_<slug>_claude-code-brief.md`, (2) logs an `[andon→Code]` inbox pointer, (3) hands Yuriy a short copy-paste prompt with the brief path. Codified as **`skills/delegate-to-code/SKILL.md`**. Code must register it in START-HERE's skills lookup so Bob triggers it (added to the 2026-06-16 brief, Task 2A.4).
- 2026-06-16 [andon→Code] **NEXT-SESSION CODE TASK queued:** `docs/2026-06-16_bootstrap-and-commit_claude-code-brief.md`. Code-only (host git + `.claude/` write, which Cowork can't reach): (1) implement K-003 (promote `SOP-triage`, reword `/prep-today` Step 3, update Code memory rule + worksheet-builder pointer); (2) wire `.claude/START-HERE.md` read-order to BOTH handoff lanes + `SOP-triage` + the worksheet template; (3) **get git healthy + commit the backlog** (this session's repo edits are uncommitted = at risk). Do K-003 before START-HERE wiring.
- 2026-06-15 [problem] **Rule conflict to reconcile.** `/prep-today` (and the Code-side `wip-triage-rule.md` memory — Cowork CAN'T read it; it's in `~/.claude/projects/.../memory/`, outside the connected folder) says Step 3 **"`Alan to do` → skip / backup only."** This contradicts YV's directive to **prep ALL WIP (YV + Alan)** for throughput (assignment = sequencing, not a filter). Likely fix: prep runs on all in-scope WIP; `/prep-today` is just YV's daily *finalize/build* list. Edits must be host/Code (`.claude/commands` + Code memory are outside Cowork write). Standardization gap: a rule living only in Code's app-internal memory can't govern Cowork — canonical copy belongs in the shared repo, both lanes reference it.
- 2026-06-15 [client] (YV rule — codify) **Order triage + "don't touch done" rule.** No explicit rule today bars work on completed/submitted orders (only the never-submit/certify human gate) → codify YV's triage: (1) **source of truth = the 2026 tab of the Operational Records Google Sheet** (id `12zZgU1ULHasOrgh_WHDOME40HdqEkKIL`); the local `Operations/Operation Records.xlsx` is stale (≤ Dec 2025). (2) **Only consider `WIP` statuses — EXCEPT `WIP - Review for Submission`**, which are finished by YV/Alan (notes like "YV done DM w/comps") → leave alone. (3) **Prep ALL `WIP` work — YV *and* Alan** (not a YV-only filter); the due date + assignment note (e.g. "YV to do 6/16") is **sequencing/ownership**, used to order the build, never to skip Alan's orders. North star: **highest throughput at best, sustainable quality** — the engine feeds the whole shop so DM files build with lightning speed. GDrive stays READ-ONLY (rule #6). **KAIZEN: add to SOP-order-intake (or a new SOP-triage) + START-HERE read-order; human merges.**
- 2026-06-15 [client] (YV decision) **Two session-handoff lanes.** Cowork/Bob writes to **`Operations/Session-Handoffs/`** (NEW — Bob is sandboxed out of `.claude/` and can't write there); Claude Code keeps **`.claude/Session-Handoffs/`**. Naming `SESSION-HANDOFF_<date>_<cowork|code>[_sN].md`, **one file per session, never edit another's** (concurrent same-file edits = the git-corruption andon). README + first Cowork handoff (`…_cowork_s3.md`) are in the new folder. **KAIZEN for host/Code: update `.claude/START-HERE.md` §1 read-order to read the LATEST from BOTH lanes** — until then the Cowork lane is only discoverable via its README (START-HERE is protected; Bob can't edit it).
- 2026-06-15 [client] (YV) **Subject-Worksheet format upgrade — add a TOP "search snapshot" block** to every sheet, surfacing what's needed to start a comp search: (1) **above-grade GLA at the very top**, (2) **county-confirmed finished area, marked if it differs from MLS**, (3) **recommended comp GLA search range** (±10%), (4) **garage (Att/Det) and/or carport, highlighted**, (5) **basement total + finished sf** (or none), (6) **subject county + surrounding counties by distance** (for Navica radius). Applied today to 4025 Saldale, 7303 Crush, 319 N Sandstone. **TOMORROW (do first):** back-apply to 2725 Daphne + 3744 Norwood + rest of queue, and fold the snapshot block into the template(s) + `skills/worksheet-builder` (kaizen). Also add **parcel dimensions** (lot metes from the CoreLogic/MLS "Parcel measurements" line) to the Site section of every sheet. Pairs with the DM-complete-template adopt/merge decision still open.
- 2026-06-15 [done] Decoded DataMaster `.dma` files (113) to build the orderly DM data-collection field list. `.dma`=ZIP→**protobuf** `Appraisal` blob, self-describing (inline field-name dict). NEW repo tools: `tools/dma-decoder/` (stdlib protobuf wire-walker, read-only) + `tools/dm-collection-sheet/` (generator: one catalog → MD reference + blank HTML). Corpus: **368 DM field names** (172 in every file) cross-referenced vs MISMO XML (453 fields) + PDFs. Deliverable = `Operations/Template files/Subject-Worksheet_TEMPLATE_DM-complete.html` (same adopted format; **24 of 59 fields were gaps** the prior template missed: Census tract, Property rights, electric/gas split, basement detail, Pool, Fence, Fireplace count, Quality(Q), Actual age, itemized appliances, driveway). Analysis in `Past Reports/_analysis/_dma-decode/`. Quirk → DMA-001. **OPEN:** Yuriy to decide adopt/merge DM-complete vs keep concise template (kaizen candidate); repo tools uncommitted (host git).
- 2026-06-15 [client] (YV pref) **Standard deliverable format = the hand-built tabbed Subject-Worksheet HTML** (Subject in 1004 field order · Sale/Listing History · Comp Grid skeleton · red flag-box · NOT-CERTIFIED footer). Purpose: collect subject data fast AND hand off cleanly to Alan; reuse going forward. Blank reusable template saved at `Operations/Template files/Subject-Worksheet_TEMPLATE.html` (client-free, {{TOKEN}} placeholders). First live use: 2725 Daphne Ln (Powhatan 1004 FHA). **KAIZEN CANDIDATE:** fold this layout into `skills/worksheet-builder` + the worksheet-renderer output contract (today the skill renders via the Python renderer; this hand-built format is richer for subject hand-off). Until a human merges that kaizen, stamp the template per order.
- 2026-06-15 [done] Depersonalized the 119 Countryside use-case in FIXTURES (per YV scope = fixtures→synthetic, keep history): `appraisal-record.example.json` rebuilt as a SYNTHETIC property (100 Example Ridge Ct, Henrico — fabricated address/IDs/owner-removed/round figures); `worksheet.example.html` regenerated (0 real PII, 3 default tabs); record-assembler `tests_qa_runner.py` + README "Countryside"→"Example Ridge". Tests 14/15 pass (data/PII/determinism all green). Historical docs (ADR-002, arch-summary, plan deck, handoff §1) intentionally KEPT as point-in-time records.
- 2026-06-15 [problem] record-assembler `tests_qa_runner.py` has a cross-platform TMP-path bug: builds a Windows path that, under the Linux sandbox, becomes a literal junk dir `record-assembler/C:\Users\yuriy\VDV Appraisals/_qa_tmp` (T14 fails in sandbox; passes on Windows host). Fix for Claude Code: compute TMP via `tempfile`/`os.path`, and add `_qa_tmp/` + temp outputs to `.gitignore` so test cruft never enters the repo. (Junk dir is sandbox-only — invalid filename on Windows.)
- 2026-06-15 [learn] Depersonalize use-cases: generalize reusable rule docs (don't tie them to a real order address). Did so for worksheet-builder gate + matrix-comp-search; promoted the out-of-county-comp lesson to data-quirks **XCO-001** (county-tag + SOR-verify; out-of-county MLS rows often miss GLA → flag, never guess). OPEN: real order data still in repo (example.json/worksheet.example.html has owner name "McDonald" + real prices; record-assembler tests + README; ADR-002/arch-summary/plan-deck narrate the live order) — convert example/test fixtures to a SYNTHETIC property; decide history-doc scope.
- 2026-06-15 [client] (YV rule) Two front doors for worksheet building, same contract+gate: **`worksheet-builder` SKILL = Cowork/Bob (interactive)**; **`/build-worksheet` command = Claude Code (automated orchestrator)**. Made explicit in both files + START-HERE table + Build-B brief.
- 2026-06-15 [client] (YV rule) Worksheet **Photos and Map tabs are OPTIONAL — require Yuriy's explicit approval**. Default worksheet = Subject · Comp grid · Sale/Listing history. Renderer now defaults them OFF; opt in with `--with-photos` / `--with-map`. Captured in worksheet-builder SKILL + `/build-worksheet` command + renderer README.
- 2026-06-15 [done] Codified the DM/ACI worksheet build as `skills/worksheet-builder/SKILL.md`; the existing `/build-worksheet` command now points to it. Centerpiece = a **completeness gate** capturing the 119 Countryside lessons so data isn't missed: ≥3 (target 3–5) CLOSED comps; segregate sold vs active/pending; county-tag + verify out-of-county (Goochland) against its SOR; never emit unverified GLA (flag it); flag view/location superiority; form-specific required fields per field-map (1073 Project Info unit counts/roof/parking; 1004C HUD plate). Invoke by `/build-worksheet` or the skill name. Renders via tools/worksheet-renderer; appraiser certifies.
- 2026-06-14 [learn] Clearing the git-corruption andon (line below): the documented three `.lock` files (`index/config/HEAD.lock`) were NOT enough — the commit still failed with `cannot lock ref 'HEAD' … refs/heads/main.lock: File exists`. Had to also delete `.git/refs/heads/main.lock`. So the full stale-lock cleanup is FOUR files: `.git/{HEAD,config,index}.lock` + `.git/refs/heads/<branch>.lock`. After that, `git add -A` + commit succeeded (commit bca5575, 63 files). The K-002 work is now committed; the andon is resolved.
- 2026-06-19 [learn] DM field label "Assessor's Parcel #" is the canonical name for APN/Tax ID/PID in DataMaster — use it on all worksheets; never label it "PID / APN / Tax ID" (confusing). Internal county portal PID (e.g. actDataScout PID 1899) is a separate no-star informational row.
- 2026-06-19 [learn] Map Reference DM field: enter `GIS` when source doesn't supply a specific reference. Never leave blank.
- 2026-06-19 [learn] Walls/trim DM field: default `Wood` — safe for nearly all VA residential stock. Mark "confirm at inspection"; don't leave TBD.
- 2026-06-19 [learn] Water/Sewer: never infer from county name or rural/urban classification — pull from Matrix/MLS. "likely Well/Septic (rural)" is wrong when Matrix shows Public (confirmed: 34 Chatham Ln, Fluvanna).
- 2026-06-19 [learn] Worksheet Neighborhood tab is now a required section: Market Boundaries (N/S/E/W roads template), Market Description (housing style + amenities), Present Land Use %, One-Unit Housing price/age, Market Conditions (TBD until comps assembled). See worksheet-builder SKILL + renderer brief.
- 2026-06-14 [learn] Henrico APEX — no stable deep links; provide search URL + GPIN for comp records: `https://realestate.henrico.gov/ords/cam/f?p=510101:1` then search by address. After search, navigate to page 5 (`f?p=510101:5::::5,7,8:P5_ROWNUM,P5_SEARCH_TYPE:1,1`) to get detail; session-bound result set carries over.
- 2026-06-14 [learn] Python IS installed on the host (3.14.6, reachable from PowerShell as `python`); the Bash tool's PATH only sees the Windows Store stub at `…\WindowsApps\python.exe` which errors. Run Python via the PowerShell tool, not Bash. Also: `jsonschema` is NOT installed (stdlib-only house rule — don't pip it; use a structural check).
- 2026-06-14 [done] Build B complete & QA'd: `tools/record-assembler/assemble_record.py` + `appraisal-record.schema.json` + `/build-worksheet` orchestrator. **15/15 QA tests PASS** on Python 3.14.6, plus a real assemble→render CLI run (6-comp fixture → valid 17KB worksheet.html). Rules verified live: GLA null+flag (never guessed), BRT MLS# strip, CLOSD/PEND/ACT→closed/pending/active segregation, out-of-county flag, derived price_per_sf + gla_delta. Found+fixed a real defect: assembler now reads subject.json with `utf-8-sig` (BOM-tolerant). See `code-reviews/arch_build-B_2026-06-13.md` (Phase 0) + `arch-qa_build-B_2026-06-13.md` (Phase 3).
- 2026-06-13 [client] (YV rule) `VDV Appraisals` is the system of record; Google Drive (and any external cloud) is READ-ONLY by default. Never create/write/copy/move files on GDrive without an explicit per-task request — all outputs go under `C:\Users\yuriy\VDV Appraisals\` first. If anything ever gets written to GDrive without that request: copy → VDV Appraisals, verify, then delete from GDrive. → added as CLAUDE.md cardinal rule #6 + into the Build-B Claude Code brief.
- 2026-06-13 [learn] (YV) MLS# `BRTVA*` = Bright MLS listing surfaced through CVR MLS (the two share data). Normalize by stripping the `BRT` prefix and keeping `VA…` — e.g. `BRTVAMB2000092` → `VAMB2000092`. The `VA…` form is the canonical key that works in Bright MLS AND DataMaster for pulling data; never feed DM the `BRT…` form. → recorded in datamaster-handoff.md + property-search SKILL Step 4.
- 2026-06-13 [done] Notes-composer knowledge built from 60 past reports (architect-designed, ADR-003). NEW: `skills/notes-composer/` (SKILL + references/notes-library/{1004,1004C}/*.yaml = de-identified note templates w/ typed slots + seen_count/confidence; adjustment-playbook/playbook.yaml = typical $ per feature [Condition ~$15k/step, GLA ~$75/sf, median $8.85k]; field-map/field-map.<form>.yaml = ONE mapping, multiple lenses [mismo·pdf·aci_web·worksheet], 1004 validated, 1073/FHA stubbed unsupported). Tools: `tools/deidentify` (one-way gate, strict+locality), `tools/pair-qa`, `tools/notes-library-builder`. Pair-QA: 4 XML↔PDF pairs = 100% value match (extraction validated). Committed library re-scanned: 0 PII. Raw extraction + de-id staging + pair-QA stay in client zone (Past Reports/_analysis).
- 2026-06-13 [learn] Cowork mount sync is one-directional+laggy: bash WRITES propagate to host fine, but bash READS of files edited via the Edit tool can be stale/truncated mid-session. Workaround: run executable scripts from the OUTPUTS mount (fresh on Write) or re-Write whole files instead of Edit; trust the editor's host view for deliverables.
- 2026-06-13 [done] Arch+QA review of Build A (renderer) — APPROVED. Found & FIXED in place: (1) URL-scheme XSS (`javascript:`/`data:` url in `sources[].url` went into a live href) → added `safe_url()`, renders inert; (2) comp-grid subject column keyed by display-label strings → rebound subject+comp accessors per row so they can't desync. QA 11 tests green (determinism byte-identical, escaping, empty/null/bad-input, well-formed HTML). Report: `code-reviews/arch-qa_worksheet-renderer_2026-06-13.md`. Open hardening (non-blocking): optional `--validate` vs JSON Schema when Build B lands; `cos(lat)` map scaling; friendly error on bad JSON.
- 2026-06-13 [learn] New skill+command `/bob-arch-qa-review` (skills/arch-qa-review/SKILL.md + .claude/commands/bob-arch-qa-review.md): runs architecture review + REAL QA tests on any build before stacking on it, fixes cheap defects in place and re-verifies, writes a code-reviews/ report, review-only (never commits). Use before relying on B/C/D modules.
- 2026-06-13 [problem] Sandbox bash mount cached a TRUNCATED copy of an edited .py mid-session and would not re-sync (host file was correct via editor). Same Windows-mount flakiness family as the git andon. Workaround when it bites: verify changed logic in isolation sandbox-locally; trust the editor's view of the host file; don't trust a stale bash `wc`/compile.
- 2026-06-13 [done] Build A (Worksheet Builder pipeline) shipped: `tools/worksheet-renderer/render_worksheet.py` — stdlib-only, deterministic, no network calls; renders `appraisal-record.json` → tabbed `worksheet.html` (Subject · Comp grid [closed segregated from active/pending] · Sale/Listing history · Photos · Map [inline SVG, no tiles]). Footer always shows the review gate. Tested vs example + synthetic record. Render contract now LOCKED → Build B (comp-pull fills the record) targets this. Git commit pending on host (one-session rule).
- 2026-06-14 [learn] Zillow photos are usable for quick subject pre-fill on 1004: (1) floor material — visible in living/dining shots; (2) fireplace presence/surround style — usually appears in living room photo; (3) front porch — visible in exterior shot #1 or #2; (4) deck/patio — often visible in rear exterior shot. Cross-check with APEX sketch codes (WDK=deck, OP=open porch). Not a substitute for inspection — use to pre-populate DM and flag for field confirmation. Confirmed on 3355 Darbytown Rd: hardwood main/carpet beds, fireplace w/ white mantel confirmed, no front porch, rear deck visible.
- 2026-06-13 [learn] Chesterfield Co assessment site (chesterfield.gov) → parcel detail → Residential tab → Dimensions section (expandable) shows floor-by-floor finished areas: F1 (1st floor), F2 (2nd floor), DA (daylight basement), OH (overhang), WD (deck), OP (porch). Sum of finished stories reconciles to the ft² in the Residential Buildings header. Always expand Dimensions to validate GLA on Chesterfield comps — confirmed on 7861 Alexandria Dr (F1=608+F2=608+DA finished = 1586 ft²).
- 2026-06-13 [learn] Cowork has no /compact command (that's Claude Code CLI only). To compress a long session: start a new conversation — Cowork auto-generates a carry-forward summary. Before closing, flush learnings to inbox manually or via /compact command.
- 2026-06-13 [learn] 1073 Project Information tab — new-construction condo defaults: Project Description=Row or Townhouse; Effective Age=1; Developer in control of HOA=Yes; Management Group=Developer; Single entity >10%=Yes (builder holds unsold units, describe as such). Unit counts require HOA docs or builder disclosure — do not guess.
- 2026-06-13 [learn] 1073 tab order in DataMaster: Subject → Contract → Neighborhood → Project Site → Project Information → Project Analysis → Unit Description → Appraiser Information. Project Information and Project Analysis are the hardest — need HOA docs. Unit Description needs inspection notes.

- 2026-06-13 [learn] Chesterfield ParcelsEnriched UseCode values confirmed: CD=condo dwelling, CN=condo non-attached, TH=townhouse fee-simple, TN=townhouse non-attached, SD=single-family detached. Style TR=townhouse-row. Use UseCode IN ('CD','CN') for condo comp filter; IN ('TH','TN') for TH comps.
- 2026-06-13 [learn] StyleCraft Homes (builder at Hancock Village, Chesterfield) sells new units direct — NOT listed in CVRMLS. Zillow shows "Source: StyleCraft Homes" with no MLS#. DM can only use PID for these; enter manually in ACI if DM can't pull by PID alone.
- 2026-06-13 [learn] Zillow can map wrong parcel to new construction condo address (14651 Hancock Towns DR returned parcel 722670030900132 / $65,400 — completely wrong builder parcel). Always cross-check Zillow GLA/sale against county GIS for new-construction condos.
- 2026-06-13 [learn] To find resale MLS# on Zillow when listing page shows only the original-purchase MLS#: run JS `document.body.innerText.match(/MLS ID #\d+/gi)` — returns all MLS IDs in the sale history stack; pick the one matching the resale agent/brokerage, not the builder entry. (Used for 6424 Bilberry Aly → found resale MLS# 2519247.)
- 2026-06-13 [learn] Chesterfield ArcGIS TaxID field is esriFieldTypeDouble — always apply Math.round() when converting to string, or floating-point rounding corrupts digits on large parcel IDs.
- 2026-06-13 [idea] (YV) Target DESKTOP appraisals as the volume play: no inspection = pure data assembly = exactly what the pipeline automates; lighter than 1004/2055, so more throughput funds ops (MLS dues, Claude subs) then margin. Same engine, lighter form; appraiser still certifies (USPAP). Confirm products/forms + AMCs with Alan. → added to plan deck + open questions.
- 2026-06-13 [learn] property-search SKILL.md was edited by TWO concurrent sessions at once (this session: routing preface + ArcGIS-technique demotion; other session: Step 4 MLS# lookup, Step 5 CSV flags, Step 6 DM-import-via-computer-use, Chesterfield UseCode, ArcGIS TaxID Math.round, MAP-tab radius, luxury GLA, one-session rule). They merged cleanly THIS time — but same-file concurrent edits are exactly what zeroed .git/config. Rule: one session edits the repo at a time. Current merged SKILL is good; will be captured by the pending K-002 commit. Refactor MIGRATES the inline county/MLS specifics into adapters/registry (ADR-002 reconciliation section).
- 2026-06-13 [andon] git on appraisal-ops-hq got corrupted during CONCURRENT Cowork access: `.git/config` zeroed to NUL bytes; stale `.git/index.lock`, `.git/config.lock`, `.git/HEAD.lock` left behind (sandbox couldn't unlink them). Likely cause: two sessions touching the same repo's git + a config write racing over the Windows mount. Work product is safe as plain files; the kaizen/K-002 commit is blocked. Fix on the host with the other session CLOSED: delete the three *.lock files, recreate `.git/config`, then commit. → hard reinforcement of the one-session rule: do NOT run git writes while a second Cowork job is open.
- 2026-06-13 [client] (YV rule) DataMaster comp CSVs go in a dedicated folder `C:\Users\yuriy\VDV Appraisals\Comps files\` going forward (NOT per-order job folders). Named `<address>_comps_appraiser-single-line.csv`. Updated datamaster-handoff.md step 2. Moved existing Countryside + Hancock CSVs there.
- 2026-06-13 [learn] Comp scope for LUXURY/large subjects: a ±10% GLA band is too tight. On 119 Countryside Ln (6,662 sf) the ±10% cap (7,328 sf) excluded the BEST comp — 8721 Ruggles Rd (same subdivision, 0.6 mi, 7,560 sf, SOLD $3,100,000) — and 8705 Raleigh Manor Pl (7,521 sf). For high-end subjects widen GLA to ~+15% (and expect to bracket up). Distance did NOT need expanding; 1 mi was fine once GLA opened up. → candidate property-search SKILL tweak: make GLA band adaptive for luxury.
- 2026-06-13 [learn] Matrix RADIUS search: the reliable way to do "within N mi of address" is the MAP-tab radius search (Alan's saved search showed "within 1.01 mi of 119 C